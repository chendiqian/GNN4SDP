gnn:
  no_mp: false   # by default we use mp
  no_dual: false   # whether to predict slack variables
  hidden: 96
  num_encode_layers: 2  # this is encoding V and C nodes
  num_conv_layers: 6
  gnn_mlp_layers: 1   # this can be fixed
  num_pred_layers: 3
  norm: graphnorm
  act: relu